[project]
name = "mini-rack-printables"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "openscad-test",
    "scadm>=0.8.0",
]

[tool.uv.sources]
openscad-test = { path = "../openscad_test", editable = true }
